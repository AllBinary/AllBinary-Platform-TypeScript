//Logic will need to map from ets to ts only
//With Canvas(this.context) Component
import { Bitmap } from './Bitmap';

/*actual*/ export class Canvas
{

    public setBitmap(bitmap: Bitmap) {
        throw new Error("No Impl");
    }
    
}

